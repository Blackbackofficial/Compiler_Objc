struct Books {
   NSString *title;
   NSString *author;
   NSString *subject;
   int   book_id;
};

int main() {
    int x = 10; // declare the variable
    int y = 10;
    int z = 10;

    int x, y, z;

    x = y = z = 20;
    int x = -10; // Unary - operator used to assign -10 to a variable named x

    x = y - z; // Subtraction operator. Subtracts z from y
    x = y * 10 + z - 5 / 4;
    x++;// Increment x by 1

    x--;// Decrement x by 1
}