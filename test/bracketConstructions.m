#import <Foundation/Foundation.h>
#import "Person.h"
#import "Fellow.h"
// debug false

// // import Foundation
// int i = 8;
// // declare a (C-style) function
// int add(int num1, int num2) {
//   return num1 + num2;
// }

int main(int argc, char * argv[]) {
//     int value = 2011;
//     int i = 9;
//     int j = 8 + i;
//     value *= value++;
//     value /= value - value--;
//     value %= value % --value;
//     value += value - (--value);
//     value >>= value + ++value;
    for (NSString * language in programmingStacks) { // debug
      if (value > 89) {
        int rer = 8989;
      } else {
        int el = 5;
      }
    }

//     // NSMutableArray
//     NSMutableArray * raceTypes = [
//       [NSMutableArray alloc] init
//     ];
//
//     // NSDictionary
//     NSDictionary * placesDict = @ {
//       @"New York": @"Brooklyn", @"Sweden": @"Malmo"
//     };

    for (int asd = 1; asd <= 10; asd++) {
      NSString * placeValue = placesDict[placeKey];
    }

    if (people > 0) {
      Person * firstPerson = people[0];
    }

    while (i < 20) {
      int j = i;
      j++;
      if (i % 2 != 0) {
        continue;
      }
      break;
    }

    int foo = 10;
    do {
      int razr = foo;
      razr = 1 + --razr;
      foo = razr;
    } while (i / i > 0 && razr % razr >= 1);
  return 0;
}